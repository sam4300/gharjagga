import 'package:flutter/material.dart';


class LandDetailScreen extends StatelessWidget {
  static const routeName = ("/land_details");

  const LandDetailScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
